-- Pet Spawner GUI Script
local player = game.Players.LocalPlayer
local replicatedStorage = game:GetService("ReplicatedStorage")
local petName = "Dog" -- Make sure this pet model exists in ReplicatedStorage

-- Create GUI elements
local screenGui = script.Parent

-- Title Label
local title = Instance.new("TextLabel")
title.Text = "🐾 Pet Spawner!"
title.Size = UDim2.new(0.4, 0, 0.1, 0)
title.Position = UDim2.new(0.3, 0, 0.1, 0)
title.BackgroundColor3 = Color3.fromRGB(255, 200, 255)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(0, 0, 0)
title.Parent = screenGui

-- Spawn Button
local spawnButton = Instance.new("TextButton")
spawnButton.Text = "Spawn Pet"
spawnButton.Size = UDim2.new(0.3, 0, 0.1, 0)
spawnButton.Position = UDim2.new(0.35, 0, 0.25, 0)
spawnButton.BackgroundColor3 = Color3.fromRGB(200, 255, 200)
spawnButton.TextScaled = true
spawnButton.Font = Enum.Font.Gotham
spawnButton.TextColor3 = Color3.fromRGB(0, 0, 0)
spawnButton.Parent = screenGui

-- Button Click Function
spawnButton.MouseButton1Click:Connect(function()
	local pet = replicatedStorage:FindFirstChild(petName)
	if pet then
		local clone = pet:Clone()
		clone.Parent = workspace
		local char = player.Character
		if char and char:FindFirstChild("HumanoidRootPart") then
			clone:SetPrimaryPartCFrame(char.HumanoidRootPart.CFrame * CFrame.new(3, 0, 0))
		end
	else
		warn("Pet model '" .. petName .. "' not found in ReplicatedStorage.")
	end
end)
